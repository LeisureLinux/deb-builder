# Build output
dist/
*.deb
vendor/
**/*.gz   # compressed sources

# Local state (optional, keep if debugging issues locally)
.cached-*
.test-*

# Cloned upstream sources & local smoke-test artifacts
age/
fzf/
tmp/
