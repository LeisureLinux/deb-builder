# nwg-look 构建配方 (Phase 1 自动生成)
repo: nwg-piotr/nwg-look
package: nwg-look
summary: "GTK3 settings editor for wlroots environments like Sway"
description: |
  GTK3 settings editor for wlroots environments like Sway
  (Phase 1 auto-built for all architectures.)
homepage: https://github.com/nwg-piotr/nwg-look
section: x11

# 版本策略：锁定上游最新稳定 tag，并在 control 里记录 commit hash
version_tag: "v1.1.1"
latest_tag: "v1.1.1"
commit_hash: "29f119f40b9e4bd4831e0d97dd0f54204ef74824"
upstream_version: "1.1.1"

# Go 构建配置
ldflags: "-s -w"

# 全架构打包
target_arches:
  - amd64
  - arm64
#   - loong64  # 临时禁用: loong64/riscv64 暂不构建，声明保留待用
#   - riscv64  # 临时禁用: loong64/riscv64 暂不构建，声明保留待用

maintainer: "LeisureLinux <albertxu@freelamp.com>"
depends: []

# ⛔ 暂时禁用：依赖 gtk3 C 库，CGO_ENABLED=0 无法编译
# 待逐包修复（或改用原生工具链构建）后移除 disabled 行重新启用
disabled: true # 依赖 gtk3 C 库，CGO_ENABLED=0 无法编译
