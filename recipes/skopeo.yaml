# skopeo 构建配方 (Phase 1 自动生成)
repo: containers/skopeo
package: skopeo
summary: "Tooling to work with remote images registries"
description: |
  Tooling to work with remote images registries
  (Phase 1 auto-built for all architectures.)
homepage: https://github.com/containers/skopeo
section: devel

# 版本策略：锁定上游最新稳定 tag，并在 control 里记录 commit hash
version_tag: "v1.24.0"
latest_tag: "v1.24.0"
commit_hash: "ea7faa7ba79b2e3308259673fbd3b472198afd1a"
upstream_version: "1.24.0"

# Go 构建配置
ldflags: "-s -w"

# 全架构打包
target_arches:
  - amd64
  - arm64
#   - loong64  # 临时禁用: loong64/riscv64 暂不构建，声明保留待用
#   - riscv64  # 临时禁用: loong64/riscv64 暂不构建，声明保留待用

maintainer: "LeisureLinux <albertxu@freelamp.com>"
depends: []

# ⛔ 暂时禁用：依赖 gpgme C 库，CGO_ENABLED=0 无法编译
# 待逐包修复（或改用原生工具链构建）后移除 disabled 行重新启用
disabled: true # 依赖 gpgme C 库，CGO_ENABLED=0 无法编译
