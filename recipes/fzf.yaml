# fzf 构建配方 (Phase 1 自动生成)
repo: junegunn/fzf
package: fzf
summary: "general-purpose command-line fuzzy finder"
description: |
  general-purpose command-line fuzzy finder
  (Phase 1 auto-built for all architectures.)
homepage: https://github.com/junegunn/fzf
section: utils

# 版本策略：锁定上游最新稳定 tag，并在 control 里记录 commit hash
version_tag: "v0.74.4"
latest_tag: "v0.74.4"
commit_hash: "a140afeb4d733cad3c96a56bf6db7e26853b6757"
upstream_version: "0.74.4"

# Go 构建配置
ldflags: "-s -w"

# 全架构打包
target_arches:
  - amd64
  - arm64
#   - loong64  # 临时禁用: loong64/riscv64 暂不构建，声明保留待用
#   - riscv64  # 临时禁用: loong64/riscv64 暂不构建，声明保留待用

maintainer: "LeisureLinux <albertxu@freelamp.com>"
depends: []
