# gdu 构建配方 (Phase 1 自动生成)
repo: dundee/gdu
package: gdu
summary: "Pretty fast disk usage analyzer"
description: |
  Pretty fast disk usage analyzer
  (Phase 1 auto-built for all architectures.)
homepage: https://github.com/dundee/gdu
section: admin

# 版本策略：锁定上游最新稳定 tag，并在 control 里记录 commit hash
version_tag: "v5.37.0"
latest_tag: "v5.37.0"
commit_hash: "a3138344aba3f41f00c196bd7a994902b8dac490"
upstream_version: "5.37.0"

# Go 构建配置
ldflags: "-s -w"

# 全架构打包
target_arches:
  - amd64
  - arm64
#   - loong64  # 临时禁用: loong64/riscv64 暂不构建，声明保留待用
#   - riscv64  # 临时禁用: loong64/riscv64 暂不构建，声明保留待用

maintainer: "LeisureLinux <albertxu@freelamp.com>"
depends: []
