# textql 构建配方 (Phase 1 自动生成)
repo: dinedal/textql
package: textql
summary: "execute SQL against structured text like CSV or TSV"
description: |
  execute SQL against structured text like CSV or TSV
  (Phase 1 auto-built for all architectures.)
homepage: https://github.com/dinedal/textql/
section: devel

# 版本策略：锁定上游最新稳定 tag，并在 control 里记录 commit hash
version_tag: "2.0.3"
latest_tag: "2.0.3"
commit_hash: "1785cd353c68aa34f97627143b9c2908dfd4ea04"
upstream_version: "2.0.3"

# Go 构建配置
ldflags: "-s -w"

# 全架构打包
target_arches:
  - amd64
  - arm64
#   - loong64  # 临时禁用: loong64/riscv64 暂不构建，声明保留待用
#   - riscv64  # 临时禁用: loong64/riscv64 暂不构建，声明保留待用

maintainer: "LeisureLinux <albertxu@freelamp.com>"
depends: []

# ⛔ 暂时禁用：上游已死代码，sqlite3 依赖损坏无法修复
# 待逐包修复（或改用原生工具链构建）后移除 disabled 行重新启用
disabled: true # 上游已死代码，sqlite3 依赖损坏无法修复
