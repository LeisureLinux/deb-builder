# gum 构建配方 (Phase 1 自动生成)
repo: charmbracelet/gum
package: gum
summary: "Tool for glamorous shell scripts 🎀"
description: |
  Tool for glamorous shell scripts 🎀
  (Phase 1 auto-built for all architectures.)
homepage: https://github.com/charmbracelet/gum
section: devel

# 版本策略：锁定上游最新稳定 tag，并在 control 里记录 commit hash
version_tag: "v2.0.0"
latest_tag: "v2.0.0"
commit_hash: "4d089f95507708a71f64dacfe7ca513219dd5267"
upstream_version: "2.0.0"

# Go 构建配置
ldflags: "-s -w"

# 全架构打包
target_arches:
  - amd64
  - arm64
#   - loong64  # 临时禁用: loong64/riscv64 暂不构建，声明保留待用
#   - riscv64  # 临时禁用: loong64/riscv64 暂不构建，声明保留待用

maintainer: "LeisureLinux <albertxu@freelamp.com>"
depends: []
