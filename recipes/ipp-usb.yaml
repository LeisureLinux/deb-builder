# ipp-usb 构建配方 (Phase 1 自动生成)
repo: OpenPrinting/ipp-usb
package: ipp-usb
summary: "Daemon for IPP over USB printer support"
description: |
  Daemon for IPP over USB printer support
  (Phase 1 auto-built for all architectures.)
homepage: https://github.com/OpenPrinting/ipp-usb
section: comm

# 版本策略：锁定上游最新稳定 tag，并在 control 里记录 commit hash
version_tag: "0.9.34"
latest_tag: "0.9.34"
commit_hash: "057c7ac8739e557d12796378f3c180136cb3e274"
upstream_version: "0.9.34"

# Go 构建配置
ldflags: "-s -w"

# 全架构打包
target_arches:
  - amd64
  - arm64
#   - loong64  # 临时禁用: loong64/riscv64 暂不构建，声明保留待用
#   - riscv64  # 临时禁用: loong64/riscv64 暂不构建，声明保留待用

maintainer: "LeisureLinux <albertxu@freelamp.com>"
depends: []

# ⛔ 暂时禁用：依赖 libusb C 库，CGO_ENABLED=0 无法编译
# 待逐包修复（或改用原生工具链构建）后移除 disabled 行重新启用
disabled: true # 依赖 libusb C 库，CGO_ENABLED=0 无法编译
