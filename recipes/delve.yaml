# delve 构建配方 (Phase 1 自动生成)
repo: go-delve/delve
package: delve
summary: "debugger for the Go programming language"
description: |
  debugger for the Go programming language
  (Phase 1 auto-built for all architectures.)
homepage: https://github.com/go-delve/delve
section: devel

# 版本策略：锁定上游最新稳定 tag，并在 control 里记录 commit hash
version_tag: "v1.27.2"
latest_tag: "v1.27.2"
commit_hash: "d116177dd925e085ba5dd340f8c644f4a1501a3b"
upstream_version: "1.27.2"

# Go 构建配置
ldflags: "-s -w"

# 全架构打包
target_arches:
  - amd64
  - arm64
#   - loong64  # 临时禁用: loong64/riscv64 暂不构建，声明保留待用
#   - riscv64  # 临时禁用: loong64/riscv64 暂不构建，声明保留待用

maintainer: "LeisureLinux <albertxu@freelamp.com>"
depends: []

# 主包在 cmd/dlv，避免误编译 _fixtures 测试目录
build_path: "./cmd/dlv"
