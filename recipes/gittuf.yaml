# gittuf 构建配方 (Phase 1 自动生成)
repo: gittuf/gittuf
package: gittuf
summary: "security layer for Git repositories (program)"
description: |
  security layer for Git repositories (program)
  (Phase 1 auto-built for all architectures.)
homepage: https://github.com/gittuf/gittuf
section: vcs

# 版本策略：锁定上游最新稳定 tag，并在 control 里记录 commit hash
version_tag: "v0.16.0"
latest_tag: "v0.16.0"
commit_hash: "fa3c295e1e46c1cff10aec1194edc29f19677723"
upstream_version: "0.16.0"

# Go 构建配置
ldflags: "-s -w"

# 全架构打包
target_arches:
  - amd64
  - arm64
#   - loong64  # 临时禁用: loong64/riscv64 暂不构建，声明保留待用
#   - riscv64  # 临时禁用: loong64/riscv64 暂不构建，声明保留待用

maintainer: "LeisureLinux <albertxu@freelamp.com>"
depends: []
