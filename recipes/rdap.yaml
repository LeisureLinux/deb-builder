# rdap 构建配方 (Phase 1 自动生成)
repo: openrdap/rdap
package: rdap
summary: "command line client for the RDAP protocol"
description: |
  command line client for the RDAP protocol
  (Phase 1 auto-built for all architectures.)
homepage: https://github.com/openrdap/rdap
section: net

# 版本策略：锁定上游最新稳定 tag，并在 control 里记录 commit hash
version_tag: "v0.10.2"
latest_tag: "v0.10.2"
commit_hash: "b4d5927b2905f218b29c2675e6f55ee3247a18ca"
upstream_version: "0.10.2"

# Go 构建配置
ldflags: "-s -w"

# 全架构打包
target_arches:
  - amd64
  - arm64
#   - loong64  # 临时禁用: loong64/riscv64 暂不构建，声明保留待用
#   - riscv64  # 临时禁用: loong64/riscv64 暂不构建，声明保留待用

maintainer: "LeisureLinux <albertxu@freelamp.com>"
depends: []
